---
phase: 04-weaponise-the-monetization-strategy
plan: 01
type: execute
wave: 3
depends_on: ["03-01"]
files_modified: [pubspec.yaml, android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist, lib/core/services/ad_service.dart, lib/main.dart, lib/core/providers/game_controller.dart]
autonomous: true
---

<objective>
Integrate `google_mobile_ads` and force interstitial video ads when the player is cornered by the timer (game over).

Purpose: Ad-driven hyper-casual experience; punishing failure with ads.
Output: AdMob initialized, test IDs configured, AdService created, and interstitial ad shown on game over state.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weaponise-the-monetization-strategy/DISCOVERY.md

@pubspec.yaml
@android/app/src/main/AndroidManifest.xml
@ios/Runner/Info.plist
@lib/core/providers/game_controller.dart
@lib/main.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add AdMob Dependency & Native Config</name>
  <files>pubspec.yaml, android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist</files>
  <action>Add `google_mobile_ads: ^6.0.0` to pubspec.yaml. Before doing this, verify breaking changes between 5.x and 6.0.0 that could affect `InterstitialAd`, `FullScreenContentCallback`, or `MobileAds.instance.initialize()`. Audit code paths using those symbols, update method signatures/callbacks, run pub get and ensure tests pass before committing.
In AndroidManifest.xml (inside `<application>`), add `<meta-data android:name="com.google.android.gms.ads.APPLICATION_ID" android:value="__ADMOB_ANDROID_APP_ID__"/>`.
In Info.plist, add `<key>GADApplicationIdentifier</key><string>__ADMOB_IOS_APP_ID__</string>`. 
Implement a CI/substitution script step (or detail in the README) that replaces these tokens (`__ADMOB_ANDROID_APP_ID__` and `__ADMOB_IOS_APP_ID__`) with production values from environment variables or `--dart-define` at build time. Ensure no production keys are committed to the repo.</action>
  <verify>`flutter pub get` and verify files syntactically correct.</verify>
  <done>AdMob configuration properties injected correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Create AdService</name>
  <files>lib/core/services/ad_service.dart</files>
  <action>Create an `AdService` class that calls `MobileAds.instance.initialize()` and exposes `loadInterstitialAd()` and `showInterstitialAd()` (using the provided test ad unit IDs). Keep a local reference to the loaded `InterstitialAd`. Set its `fullScreenContentCallback` to nullify the reference once dismissed, and reload a new one. Register this service strictly as a Riverpod `Provider` (e.g. `final adServiceProvider = Provider<AdService>(...)`) so all usage and tests consume it via Riverpod. Do not use a plain singleton.</action>
  <verify>`flutter analyze lib/core/services/ad_service.dart` passes.</verify>
  <done>AdService handles loading, displaying, and reloading interstitial ads.</done>
</task>

<task type="auto">
  <name>Task 3: Trigger Ads on Game Over</name>
  <files>lib/main.dart, lib/core/providers/game_controller.dart</files>
  <action>Initialize the MobileAds SDK in `main.dart` inside `main()`. In `GameController`, call `AdService.loadInterstitialAd()` during `initializeGame()`. When the timer ticks down to 0 and `isGameOver` is triggered, show the interstitial ad by calling `AdService.showInterstitialAd()`. To prevent multiple interstitials, add a boolean flag `_adShown` (initialized to false), set it to true right before calling `showInterstitialAd()`, and check it (`if (_adShown) return`) so repeated timer callbacks cannot trigger it again. Reset `_adShown` to false only when appropriate (e.g. on new game).</action>
  <verify>flutter analyze passes.</verify>
  <done>A punishing ad plays when the user runs out of time.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze` succeeds
- [ ] `flutter build ios --simulator --no-codesign` succeeds
- [ ] `flutter build apk` succeeds
</verification>

<success_criteria>
- All tasks completed
- AdMob configured properly without breaking compilation
- Interstitials are requested and shown natively upon Game Over
</success_criteria>

<output>
After completion, create `.planning/phases/04-weaponise-the-monetization-strategy/04-01-SUMMARY.md`
</output>
