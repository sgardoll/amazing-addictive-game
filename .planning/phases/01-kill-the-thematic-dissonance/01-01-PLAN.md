---
phase: 01-kill-the-thematic-dissonance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "lib/core/models/emotion.dart",
  "lib/core/models/ingredient.dart",
  "lib/core/models/bottle.dart",
  "lib/core/models/tray.dart",
  "lib/core/models/game_state.dart",
  "lib/core/models/level.dart",
  "lib/core/services/level_generator.dart",
  "lib/core/providers/game_controller.dart",
  "lib/core/providers/achievement_controller.dart",
  "lib/core/providers/daily_challenge_controller.dart",
  "lib/features/game/widgets/emotion_bottle.dart",
  "lib/features/game/widgets/ingredient_tray.dart",
  "lib/features/game/widgets/game_board.dart"
]
autonomous: true
---

<objective>
Rename and rebrand core entities from abstract "colors in bottles" to tangible "ingredients on trays", fulfilling Phase 1's goal of dismantling the thematic dissonance.

Purpose: Prepares the domain layer and state for a chaotic fast-food order fulfillment game.
Output: Core data models and controllers use the new domain concepts (Ingredient and Tray) instead of Emotion and Bottle.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kill-the-thematic-dissonance/01-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Rename Models and Enums</name>
  <files>
    lib/core/models/emotion.dart,
    lib/core/models/ingredient.dart,
    lib/core/models/bottle.dart,
    lib/core/models/tray.dart
  </files>
  <action>
    - Rename `emotion.dart` to `ingredient.dart`. Change enum `Emotion` to `Ingredient`. Update the 6 values to fast-food items (e.g., burger, fries, soda, pizza, hotdog, donut) with corresponding emojis and thematic colors (reds, yellows, oranges).
    - Rename `bottle.dart` to `tray.dart`. Change class `Bottle` to `Tray`.
    - Update methods in `Tray`: `canPourTo` -> `canMoveTo`, `pourableAmount` -> `movableAmount`.
    - Delete `emotion.dart` and `bottle.dart` completely.
  </action>
  <verify>ls lib/core/models/ingredient.dart lib/core/models/tray.dart succeeds</verify>
  <done>Models are renamed and refactored</done>
</task>

<task type="auto">
  <name>Task 2: Refactor State and Providers</name>
  <files>
    lib/core/models/game_state.dart,
    lib/core/models/level.dart,
    lib/core/services/level_generator.dart,
    lib/core/providers/game_controller.dart,
    lib/core/providers/achievement_controller.dart,
    lib/core/providers/daily_challenge_controller.dart
  </files>
  <action>
    - Refactor these files to import and use `Ingredient` and `Tray` instead of `Emotion` and `Bottle`.
    - Rename variables: `bottles` -> `trays`, `selectedBottle` -> `selectedTray`, `completeBottles` -> `completeTrays`, `totalBottles` -> `totalTrays`.
    - In `AchievementController` and `DailyChallengeController`, update any hardcoded text like "emotion_master" to "order_master" or "fast_food_master".
    - Avoid changing the underlying game logic (win conditions stay the same for now), just re-map the domain language.
  </action>
  <verify>flutter analyze lib/core/ passes</verify>
  <done>Core business logic fully migrated to new entities</done>
</task>

<task type="auto">
  <name>Task 3: Refactor UI Widgets and Imports</name>
  <files>
    lib/features/game/widgets/emotion_bottle.dart,
    lib/features/game/widgets/ingredient_tray.dart,
    lib/features/game/widgets/game_board.dart
  </files>
  <action>
    - Rename `emotion_bottle.dart` to `ingredient_tray.dart`.
    - Rename the `EmotionBottle` widget class to `IngredientTray`.
    - Update all property names: `bottle` -> `tray`, `selectedBottleIndex` -> `selectedTrayIndex`.
    - Update `game_board.dart` to import `ingredient_tray.dart` and use `IngredientTray` widgets.
    - Delete `emotion_bottle.dart`.
    - Ensure `flutter analyze` passes across the whole project. No need to rewrite the fluid animation logic yet, just get the renamed code compiling.
  </action>
  <verify>flutter analyze && flutter test</verify>
  <done>All UI widgets reflect the renamed domain models and project compiles without errors</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze` passes
- [ ] Model files renamed successfully (no old `emotion.dart` or `bottle.dart` left)
- [ ] App builds successfully
</verification>

<success_criteria>
- The domain vocabulary is successfully shifted to "Ingredient" and "Tray".
- Code compiles and tests pass without any remaining references to "Bottle" or "Emotion".
</success_criteria>

<output>
After completion, create `.planning/phases/01-kill-the-thematic-dissonance/01-01-SUMMARY.md`
</output>
