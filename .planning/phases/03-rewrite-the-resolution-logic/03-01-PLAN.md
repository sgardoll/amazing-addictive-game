---
phase: 03-rewrite-the-resolution-logic
plan: 01
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [lib/core/providers/game_controller.dart, lib/features/game/widgets/customer_view.dart, lib/features/game/widgets/game_board.dart, lib/features/game/widgets/ingredient_tray.dart]
autonomous: true
---

<objective>
Change the win condition to an active order-fulfillment model (drag-to-submit or tap-to-serve) that validates completed bottles against active Customer orders.

Purpose: Instead of clearing the board, the player must actively serve completed trays to waiting customers.
Output: UI displaying customers, interaction on completed trays to serve them, and win logic updated to target "customers served".
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/core/providers/game_controller.dart
@lib/features/game/widgets/game_board.dart
@lib/features/game/widgets/ingredient_tray.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add serve logic to GameController</name>
  <files>lib/core/providers/game_controller.dart</files>
  <action>Update `GameStateData` with `int customersServed`, `int targetCustomers` (e.g., 5), and a boolean `isWon` flag. Add a method `serveTray(int trayIndex)`. If the tray at `trayIndex` is complete (`isComplete`), find the oldest customer whose `order` matches the tray's ingredient by selecting the first matching customer in the customers list (lowest index, assuming customers are appended on arrival). If found, remove that customer, clear the tray, increment `customersServed`, and check if `customersServed >= targetCustomers`. If so, set `GameStateData.isWon = true` and stop the timer.</action>
  <verify>flutter analyze passes.</verify>
  <done>Game state holds the score and serving logic.</done>
</task>

<task type="auto">
  <name>Task 2: Build CustomerView widget</name>
  <files>lib/features/game/widgets/customer_view.dart</files>
  <action>Create a new `CustomerView` stateless widget that displays a customer's requested ingredient (e.g., its emoji) and a progress bar or indicator mapping `currentPatience` relative to `maxPatience`. Change color to red if patience is low.</action>
  <verify>Widget compiles cleanly without syntax errors.</verify>
  <done>CustomerView is built and ready for inclusion in GameBoard.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate serving flow into UI</name>
  <files>lib/features/game/widgets/game_board.dart, lib/features/game/widgets/ingredient_tray.dart</files>
  <action>In `game_board.dart`, render the `List<Customer>` using the `CustomerView` component at the top of the screen (e.g., in an `AppBar` or `Row` above the trays). In `ingredient_tray.dart` (or within the `GestureDetector` that selects trays), add logic: if the tray is complete and tapped, call `ref.read(gameControllerProvider.notifier).serveTray(index)` instead of selecting it for pouring.</action>
  <verify>flutter build ios --simulator --no-codesign (or android build) completes without errors.</verify>
  <done>The game UI now accurately represents an order-fulfillment flow.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze` succeeds
- [ ] UI properly handles customer rendering and tap-to-serve logic
</verification>

<success_criteria>
- All tasks completed
- Tap-to-serve mechanic allows clearing trays and fulfilling customers
- Win state triggers when target customer count is reached
</success_criteria>

<output>
After completion, create `.planning/phases/03-rewrite-the-resolution-logic/03-01-SUMMARY.md`
</output>
