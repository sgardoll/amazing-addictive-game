---
phase: 02-inject-the-chaos-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/core/models/customer.dart, lib/core/providers/game_controller.dart]
autonomous: true
---

<objective>
Introduce the real-time reactive loop with a Customer model and decay timer.

Purpose: Convert the game from sequential moves to real-time chaos.
Output: Customer model defined, GameStateData extended, and a ticking GameController loop that decrements patience.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/core/models/ingredient.dart
@lib/core/providers/game_controller.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Customer model</name>
  <files>lib/core/models/customer.dart</files>
  <action>Create an immutable `Customer` class (freezed or simple Dart class). It needs an `id` (String), an `order` (Ingredient), `maxPatience` (int), and `currentPatience` (int). Include a copyWith method or use freezed.</action>
  <verify>flutter analyze passes</verify>
  <done>Customer model is defined and exportable.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Timer and State into GameController</name>
  <files>lib/core/providers/game_controller.dart</files>
  <action>Update `GameStateData` to include `List<Customer> activeCustomers` (default empty) and `bool isGameOver` (default false). 
Update `GameController` to hold a `Timer? _gameTicker`. In `initializeGame`, start a periodic timer (1 second interval). On each tick: decrement `currentPatience` for all active customers. If any customer's patience reaches 0, set `isGameOver = true` and cancel the timer. Spawn a new customer randomly every few seconds if `activeCustomers` length is less than 3.</action>
  <verify>flutter test (if applicable) or flutter analyze passes.</verify>
  <done>GameController ticks every second, reducing patience and triggering game over when patience reaches 0.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze` succeeds
- [ ] Game state builds successfully
</verification>

<success_criteria>
- All tasks completed
- GameController possesses a real-time reactive loop
</success_criteria>

<output>
After completion, create `.planning/phases/02-inject-the-chaos-engine/02-01-SUMMARY.md`
</output>
